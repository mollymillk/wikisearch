import React from 'react';
import ReactDOM from 'react-dom/client';
import WikiApi from './components/wiki';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
      <WikiApi/>
    </React.StrictMode>
);


